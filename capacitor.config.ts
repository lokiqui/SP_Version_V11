import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ionic.skypractice',
  appName: 'skypractice',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
